# -*- coding: utf-8 -*-
"""demographic_data_analyzer.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1yC4js5J1kLojhKw0mwDziHdvy8JPv7CO

How many people of each race are represented in this dataset?

    race
    White	27815
    Black	3124
    Asian-Pac-Islander	1039
    Amer-Indian-Eskimo	311
    Other	271


This should be a Pandas series with race names as the index labels. (race column)

What is the average age of men?


    Male	39.433567


What is the percentage of people who have a Bachelor's degree?


    education
    Bachelors	16.443489

What percentage of people with advanced education (Bachelors, Masters, or Doctorate) make more than 50K?

    Bachelors 6.821253
    Doctorate	0.939803
    Masters 	2.945332

What percentage of people without advanced education make more than 50K?


    class
    <=50K	82.6286
    >50K	17.3714

What is the minimum number of hours a person works per week?

    min 1.000000

What percentage of the people who work the minimum number of hours per week have a salary of more than 50K?

    10.0

What country has the highest percentage of people that earn >50K and what is that percentage?

    United-States	22.023956

Identify the most popular occupation for those who earn >50K in India.

    Prof-specialty	25.0
"""

import pandas as pd
import numpy as np

df = pd.read_csv("/content/adult.data")
df.columns=['age','workclass','fnlwgt','education','education-num','marital-status','occupation','relationship','race','sex','capital-gain','capital-loss','hours-per-week','native-country','class']

df[['race']].value_counts()

pd.pivot_table(df, index=['sex'], values=['age'], aggfunc='mean')

df[['education']].value_counts()/df.shape[0]*100

pd.pivot_table(df, index=['education'], columns=['class'], values=['age'], aggfunc='size')/df.shape[0] *100

pd.pivot_table(df.loc[df['education'] != ' Doctorate'].loc[df['education'] != ' Masters'].loc[df['education'] != ' Bachelors'], index=['education'], columns=['class'], values=['age'], aggfunc='size').sum()/pd.pivot_table(df.loc[df['education'] != ' Doctorate'].loc[df['education'] != ' Masters'].loc[df['education'] != ' Bachelors'], index=['education'], columns=['class'], values=['age'], aggfunc='size').sum().sum() *100

df.describe()

df['class'] = df['class'].astype("string")
len(df.loc[df['class']==' >50K'].loc[df['hours-per-week'] == 1])/ len(df.loc[df['hours-per-week'] == 1]) *100

pd.pivot_table(df, index=['native-country'], columns=['class'], values=['age'], aggfunc='count') / df.shape[0] *100

pd.pivot_table(df.loc[df['native-country'] == ' India'], index=['occupation'], columns=['class'], values=['age'], aggfunc='count') / df.loc[df['native-country'] == ' India'].shape[0] *100